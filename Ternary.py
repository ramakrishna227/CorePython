hungry = 1  # we can use 1 or True OR we can use 0 or False
x = 'Feed the hungry' if hungry else 'not required to feed any'  # ternary conditional operator in python
print(x)

# we can use None for else clause
